"""
This is a setup.py script generated by py2applet

Usage:
    python setup.py py2app
"""

from setuptools import setup
import os.path

NAME = 'xig'
VERSION = '1.5.0a1'
SRC_DIR = "../../src"

APP = os.path.join(SRC_DIR, './gui/xig_app.py')
DATA_FILES = ['gui', 'library', 'sessions', 'xig.py', 'xig_config_default.py']
DATA_FILES = map(lambda p: os.path.join(SRC_DIR, p), DATA_FILES)
OPTIONS = {'argv_emulation': True, 'iconfile': 'xig.icns',
           'includes': ['uuid', 'Queue', 'urllib2', 'cgi', 'io',
                        'wsgiref', 'wsgiref.simple_server',
                        'xml.etree', 'xml.etree.cElementTree', 'xml.etree.ElementTree']}

plist = dict(
    CFBundleIconFile=NAME,
    CFBundleName=NAME,
    CFBundleShortVersionString=VERSION,
    CFBundleGetInfoString=' '.join([NAME, VERSION]),
    CFBundleExecutable=NAME,
    CFBundleIdentifier='com.google.code.p.xig',
)

setup(
    app=[
        dict(script=APP, plist=plist),
    ],
    data_files=DATA_FILES,
    options={'py2app': OPTIONS},
    setup_requires=['py2app'],
)
